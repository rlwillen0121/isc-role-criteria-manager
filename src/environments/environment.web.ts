export const APP_CONFIG = {
  production: false,
  environment: 'WEB'
};

export const environment = {
  production: false,
  web: true,
  webApiUrl: 'http://localhost:3000/api', // URL for the web API with full host and port
};
